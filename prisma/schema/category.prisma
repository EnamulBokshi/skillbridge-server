model Category {
  id         String    @id @default(uuid())
  name       String
  slug       String
  slot_price Int
  subject    Subject[]
  createdAt  DateTime  @default(now())
  updatedAt  DateTime  @default(now())

  @@map("category")
}
